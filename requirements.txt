anthropic>=1.4
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
python-dotenv>=1.0

# Slack front end (slack_bot.py) — not needed for the web app or CLI
slack-bolt>=1.20
aiohttp>=3.9

# MCP server (storygen.mcp_server) — not needed for the web app, Slack or CLI.
# v2 renamed FastMCP to MCPServer, which is the API this uses.
mcp>=2.0

# dev / test
pytest>=8.0
pytest-asyncio>=0.23
